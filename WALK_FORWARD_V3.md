# Walk Forward V3

每项研究包含 5 个滚动时间窗口。前半段为初始训练区，其后每窗将训练尾部约 15% 作为独立验证集，再在后续连续区间测试；训练和验证中跨越下一边界的标签会被 purge。

判定关注中位数、均值、标准差、最差窗口、正 IC 窗口比例和 ICIR，而非单个最好窗口。10 项研究均完成 5 个有效窗口。

相对较好的线索为美股 T+20 Rank IC 中位数 0.0615、正窗口 80%，但最差窗口 -0.0386；美股 T+5 中位数 0.0269、正窗口 80%，最差窗口 -0.0334。A股 T+5 中位数 0.0449，但最差窗口 -0.0904。Crypto 的 24H 和 7D 中位数为负。

因此没有模型满足“正且稳定、最差窗口可接受、基线胜出、成本后有效、多资产稳定”的联合门禁。
