# AI行情助手 v1.7.0 新闻情报与事件回测审计

## 根因

- v1.6.1 的 4 个业务 Provider 实际全部请求 Google News RSS；该源在当前网络环境出现 `ConnectError` 时，页面会同时失去所有实时新闻。
- `save_news_intelligence` 曾把一次资产查询返回的全部市场/宏观新闻写成该资产的 `query-target` 关系，造成股票关联污染。
- 仅按标题与链接哈希去重，没有相似转载合并、筛选分页、Provider 健康状态或数据库降级。
- 原回测只使用当次抓到的近期新闻，缺少历史公告采集、T+10、方向/阈值筛选及结果持久化。

## v1.7.0 数据链

真实 Provider 彼此独立：东方财富交易所公告聚合、CoinDesk RSS、Cointelegraph RSS、CNBC Markets RSS、BBC Business RSS、Yahoo Finance RSS。任何源失败均记录状态；数据库中已持久化的真实新闻作为明确标记的降级结果。

分析层是 `financial-event-rules-v2` 确定性规则与实体匹配引擎，不是 LLM 或 FinBERT。输出包括方向、分数、置信度、事件类型、原因、明确提及的股票、板块、直接/间接/反向影响。没有识别到实体时保持空列表，不强行关联。

回测仅使用带不可变发布时间的已持久化新闻/公告。以首根严格晚于发布时间的真实日 K 开盘价作为入场；T+1/T+3/T+5/T+10/T+20 均按真实交易 bar 计算，不按自然日偏移。少于 10 个完成样本时只返回审计明细并显示“数据不足”。

## 2026-09-07 实测

- 默认实时采集：119 条，6/6 Provider 成功；API 分页每页 20 条。
- 再次 API 验收时数据库中可筛选真实新闻：190 条，首屏 20 条，Provider 错误 0。
- 贵州茅台（600519）：59 条已持久化明确关联新闻/公告；抽查首屏 20/20 均含 `600519` 关系。
- 历史公告：59 条；真实日 K：641 根（腾讯证券备用源）。
- 偏多、无额外 Impact 阈值、T+5：11 个完成样本，方向胜率 54.55%，平均标的收益 0.1325%；11 条结果已写入 `historical_events`。
- 偏多、Impact >= 70、T+5：0 个完成样本，正确返回数据不足，不发布胜率。
- 自动化：53 项非网络测试通过；Vue 生产构建通过。

以上数字是本次运行时观测值，会随真实新闻、公告和行情更新而变化，不是产品承诺或投资建议。
